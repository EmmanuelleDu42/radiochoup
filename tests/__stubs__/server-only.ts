// Stub for `server-only` package in vitest environment.
// In Next.js production, this package throws at import time when used in client bundles.
// In tests (Node environment), we simply no-op it.
export {};
